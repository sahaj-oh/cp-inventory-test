# secrets / local env
backend/.env
frontend/.env
*.local

# Local dev-only OTP bypass (never deployed). When these files are absent
# (production) the imports fail softly and login stays strictly OTP-only.
backend/local_bypass.py
frontend/src/local_bypass.js

# python
backend/venv/
**/__pycache__/
*.pyc
.pytest_cache/

# node / build
node_modules/
dist/
frontend/dev-dist/

# os / editor
.DS_Store

# sdd scratch
.superpowers/
graphify-out/